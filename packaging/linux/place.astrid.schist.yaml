# Flatpak manifest. Build with:
#   flatpak-builder --user --install build packaging/linux/place.astrid.schist.yaml
app-id: place.astrid.schist
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: schist
finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --device=dri
  # Schist only touches files the user picks through the portal.
  - --filesystem=xdg-pictures
  - --filesystem=xdg-documents
build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
  env:
    CARGO_HOME: /run/build/schist/cargo
modules:
  - name: schist
    buildsystem: simple
    build-commands:
      - cargo --offline build --release -p schist-app
      - install -Dm755 target/release/schist /app/bin/schist
      - install -Dm644 packaging/linux/schist.desktop
          /app/share/applications/place.astrid.schist.desktop
      - install -Dm644 packaging/linux/schist.png
          /app/share/icons/hicolor/256x256/apps/place.astrid.schist.png
    sources:
      - type: dir
        path: ../..
